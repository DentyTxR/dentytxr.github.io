@using System.Diagnostics
@inject NavigationManager NavManager
@inject IJSRuntime JSRuntime

@page "/"
<PageTitle>Home</PageTitle>

<MudContainer MaxWidth="MaxWidth.Large" Class="mt-8">
    <MudText>Nothing here, yet.</MudText>
    <br />

    <MudText>I've started development on a new website project that will more interaction and have server features, as this current one is limited due WASM.</MudText>
    <MudButton Variant="Variant.Filled" Color="Color.Primary" Href="https://denty.dev/" Target="_blank">
        New website (WIP)</MudButton>
    <br/>
    <br/>
    <br/>
    <br/>

    <MudImage Src="https://media1.giphy.com/media/v1.Y2lkPTc5MGI3NjExZTRnMjAwZ2oxY2lqaWEzcXZra3FkajQ1aHRibHJleDFtM2lycHRmNiZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/VbnUQpnihPSIgIXuZv/giphy.gif" />
</MudContainer>

@* <MudButton OnClick="LoginWithDiscord">
    Login with Discord
</MudButton>


@code {
    private async Task LoginWithDiscord()
    {
        // Generate PKCE values
        var verifier = Utils.PkceHelper.GenerateCodeVerifier();
        var challenge = Utils.PkceHelper.GenerateCodeChallenge(verifier);

        await JSRuntime.InvokeVoidAsync("sessionStorage.setItem", "discord_code_verifier", verifier);

        var clientId = "1541279443740262483";

        var redirectUri = NavManager.BaseUri + "callback";
        Debug.WriteLine($"EXACT REDIRECT URI: {redirectUri}");
        var encodedRedirectUri = Uri.EscapeDataString(redirectUri);

        var authUrl = $"https://discord.com/oauth2/authorize?client_id={clientId}&redirect_uri={encodedRedirectUri}&response_type=code&scope=identify&code_challenge={challenge}&code_challenge_method=S256";

        NavManager.NavigateTo(authUrl);
    }
} *@